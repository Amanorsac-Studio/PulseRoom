name: Build installers

# Builds: Windows installer, macOS universal DMG, Android APK, iOS (.ipa).
# The iOS build is signed automatically when the signing secrets are present,
# and falls back to an unsigned build when they are not.
# Trigger manually (Actions -> Run workflow) or by pushing a tag like v1.0.3.

on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npx electron-builder --win --publish never
      - uses: actions/upload-artifact@v4
        with:
          name: PulseRoom-Windows
          path: dist/*.exe

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: rm -f package-lock.json && npm install --no-audit --no-fund
      - run: npm install dmg-license --no-save || true
      - run: npx electron-builder --mac --universal --publish never
      - uses: actions/upload-artifact@v4
        with:
          name: PulseRoom-macOS
          path: dist/*.dmg

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: echo "APP_VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_ENV
      - run: npm install --no-audit --no-fund
      - run: npm run build:www
      - run: npx cap add android
      - name: Generate app icons and splash
        run: |
          mkdir -p assets && cp icon.png assets/logo.png
          npx @capacitor/assets generate --android \
            --iconBackgroundColor '#0c0e12' --iconBackgroundColorDark '#0c0e12' \
            --splashBackgroundColor '#0c0e12' --splashBackgroundColorDark '#0c0e12' \
            || echo "asset generation skipped"
      - run: npx cap sync android
      - run: cd android && chmod +x gradlew && ./gradlew assembleDebug --no-daemon
      - run: cp android/app/build/outputs/apk/debug/app-debug.apk "PulseRoom-${APP_VERSION}-Android.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: PulseRoom-Android
          path: PulseRoom-*-Android.apk

  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: echo "APP_VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_ENV
      - run: npm install --no-audit --no-fund
      - run: npm run build:www
      - run: npx cap add ios
      - name: Generate app icons and splash
        run: |
          mkdir -p assets && cp icon.png assets/logo.png
          npx @capacitor/assets generate --ios \
            --iconBackgroundColor '#0c0e12' --iconBackgroundColorDark '#0c0e12' \
            --splashBackgroundColor '#0c0e12' --splashBackgroundColorDark '#0c0e12' \
            || echo "asset generation skipped"
      - run: npx cap sync ios

      # Signing. Repository and organization secrets both resolve through
      # `secrets.*`, and several common names are accepted so whatever the
      # secrets are already called should be picked up without renaming.
      - name: Build iOS (signed when credentials are present)
        id: iosbuild
        env:
          IOS_CERT_B64: ${{ secrets.IOS_CERTIFICATE_BASE64 || secrets.BUILD_CERTIFICATE_BASE64 || secrets.APPLE_CERTIFICATE_BASE64 || secrets.IOS_DIST_CERT_BASE64 || secrets.CERTIFICATE_BASE64 || secrets.APPLE_DISTRIBUTION_CERT_BASE64 }}
          IOS_CERT_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD || secrets.P12_PASSWORD || secrets.APPLE_CERTIFICATE_PASSWORD || secrets.CERT_PASSWORD || secrets.IOS_P12_PASSWORD }}
          IOS_PROFILE_B64: ${{ secrets.IOS_PROVISION_PROFILE_BASE64 || secrets.BUILD_PROVISION_PROFILE_BASE64 || secrets.APPLE_PROVISIONING_PROFILE_BASE64 || secrets.PROVISIONING_PROFILE_BASE64 || secrets.IOS_PROFILE_BASE64 || secrets.IOS_MOBILEPROVISION_BASE64 }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID || secrets.IOS_TEAM_ID || secrets.TEAM_ID }}
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID || secrets.ASC_KEY_ID || secrets.APPSTORE_KEY_ID || secrets.APPLE_API_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID || secrets.ASC_ISSUER_ID || secrets.APPSTORE_ISSUER_ID || secrets.APPLE_API_ISSUER_ID }}
          ASC_PRIVATE_KEY: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY || secrets.ASC_PRIVATE_KEY || secrets.APPSTORE_PRIVATE_KEY || secrets.APPLE_API_PRIVATE_KEY }}
        run: |
          # Presence report only — never prints a secret value.
          report() { if [ -n "${2:-}" ]; then echo "  $1: present"; else echo "  $1: not set"; fi; }
          echo "Signing credentials visible to this run:"
          report "certificate (.p12 base64)" "${IOS_CERT_B64:-}"
          report "certificate password"      "${IOS_CERT_PASSWORD:-}"
          report "provisioning profile"      "${IOS_PROFILE_B64:-}"
          report "team id (optional)"        "${APPLE_TEAM_ID:-}"
          report "App Store Connect key id"  "${ASC_KEY_ID:-}"
          report "App Store Connect issuer"  "${ASC_ISSUER_ID:-}"
          report "App Store Connect key"     "${ASC_PRIVATE_KEY:-}"
          echo
          chmod +x scripts/ios-build.sh
          bash scripts/ios-build.sh

      - uses: actions/upload-artifact@v4
        with:
          name: PulseRoom-iOS${{ steps.iosbuild.outputs.ios_signed == 'true' && '-signed' || '-unsigned' }}
          path: PulseRoom-*-iOS*.ipa

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: ios-build-log
          path: ios-build.log
          if-no-files-found: ignore
